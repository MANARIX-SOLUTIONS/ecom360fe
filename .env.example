# API base URL (backend)
# Dev: unset = use http://localhost:8080 directly (avoids proxy 404s)
# Set VITE_API_URL= to use Vite proxy (/api → localhost:8080)
# Prod: required, e.g. https://api.yourdomain.com
# VITE_API_URL=
